package org.academiadecodigo.queuecumbers.Game;
import org.academiadecodigo.queuecumbers.GameObjects.Enemies;
import org.academiadecodigo.queuecumbers.GameObjects.HitableObjects;
import org.academiadecodigo.queuecumbers.GameObjects.Player;
import org.academiadecodigo.queuecumbers.SimpleGfx.FieldGfx;
import java.util.Scanner;
public class Game {

/*
    private String playerName;
    private int timer = 0;
    public Game (){
        Scanner input = new Scanner (System.in);
        System.out.println("Enter player name");
        playerName = input.nextLine();
        System.out.println("You've chosen the name " + playerName);
        Player newPlayer = new Player();
        // experimentei incluir um playerName aqui, mas foi só para ter algo no Game();
    }
    public void init() throws InterruptedException {
        FieldGfx background = new FieldGfx();
        background.fieldGfx();
        //Enemies bomb =
        new Enemies();
        while (true) {
            Thread.sleep(50);
            timer++;
            if(timer%100 == 0) {
                // Enemies bomb2 = new Enemies();
                // bomb2.moveLeft();
                // óbvio erro de compilação "cannot resolve symbol bomb2",
                // esta variável não é inicializada até mais tarde;
                new Enemies();
                System.out.println("a new bomb has been created!");
            }
            //HitableObjects.moveLeft();
            // bomb.moveLeft();
            Enemies.moveLeft(); // tentei moveLeft() como método estático para que fosse "moveAllObjects()"
            // mas se for a classe inteira a mover movem-se apenas "1" cada um;
            // mantenho que irá ser necessário um array com um nº limitado de objectos
            // (pode ser um limite muito alto) por forma a ser possível ir criando novos e
            // movê-los.
            // cagámos de vez na classe "GameObjects"??
        }
    }

*/

}