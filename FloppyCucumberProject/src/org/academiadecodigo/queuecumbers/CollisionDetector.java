package org.academiadecodigo.queuecumbers;

import org.academiadecodigo.queuecumbers.GameObjects.Enemies;
import org.academiadecodigo.queuecumbers.GameObjects.HitableObjects;
import org.academiadecodigo.queuecumbers.GameObjects.Player;

public class CollisionDetector {

  /*
        public void checkCollision(Player cucumber, HitableObjects hitableObject){
            //Condition that verifies collision with cucumber
            if(cucumberActualX2 == hitableActualX1 && cucumberActualY1 < hitableActualY2 && cucumberActualY2 > hitableActualY1) {
                cucumber.crash() = true; // isCollided será uma propriedade do player
            }
            if(cucumberActualY1 == hitableActualY2 && cucumberActualX2 > hitableActualX1 && cucumberActualX1 < hitableActualX2) {
                cucumber.crash() = true;
            }
            if(cucumberActualY2 == hitableActualY1 && cucumberActualX2 >hitableObjectX1 && cucumberActualX1 < hitableActualX2) {
                cucumber.crash() = true;
            }
            //Condition that verifies if cucumber collided to the top of the screen
            if (cucumberActualY1 <= 0) { // se o canto superior do player for <= que a linha 0
                cucumber.crash = true;
                gameOver();
            }
            if (cucumberActualY2 >= field.GetHeight()) { // se o canto inferior  do player for >= ao limite inferior do field
                cucumber.crash = true;
                gameOver();
            }
            //Collision with an Enemy
            if (hitableObject instanceof Enemies && isCollided) {
                gameOver();
                //game over
            }
            //Collision with a collectible
            if (hitableObject instanceof Collectibles && isCollided) {
                cucumber.setScore(score++);
                hitableObject.hide();
            }
        }

   */
    }